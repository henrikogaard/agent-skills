interface:
  display_name: "Delegated Subagents"
  short_description: "Opt-in supervision of external CLI workers"
  default_prompt: "I explicitly opt in to $delegated-subagents for bounded repository work. Use external OpenCode or Devin CLI workers while this Codex task coordinates, reviews, and reports status."

policy:
  allow_implicit_invocation: false
